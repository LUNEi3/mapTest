# ----------------------------------------------------------------------
# Maven (Build Results)
# ----------------------------------------------------------------------
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties

# ----------------------------------------------------------------------
# NetBeans IDE
# ----------------------------------------------------------------------
# Ignore per-user private settings (window layout, open files, local paths)
nbproject/private/

# Ignore NetBeans build artifacts (Maven uses target/, but safer to include these)
build/
nbbuild/
dist/
nbdist/
.nb-gradle/

# Profiler snapshots
*.nps

# NOTE: We intentionally DO NOT ignore the main 'nbproject/' folder.
# This allows 'nbactions.xml' (custom run commands) to be shared with the team.
# If you want a 100% generic Maven project (no IDE files at all), 
# uncomment the line below:
# nbproject/

# ----------------------------------------------------------------------
# Java & Compiled Files
# ----------------------------------------------------------------------
*.class
*.log
*.ctxt

# ----------------------------------------------------------------------
# Operating System Metadata
# ----------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ----------------------------------------------------------------------
# Package Files
# ----------------------------------------------------------------------
# Ensure you don't accidentally commit binary packages
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# ----------------------------------------------------------------------
# Other IDEs (Optional safety net)
# ----------------------------------------------------------------------
# It is good practice to ignore other IDE files in case a teammate uses them
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
